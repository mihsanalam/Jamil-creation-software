import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function WorkOrderPage() {
  return (
    <PagePlaceholder
      title="Work Order"
      description="Turn a fabric batch into a work order and pick its phase template."
    />
  );
}
